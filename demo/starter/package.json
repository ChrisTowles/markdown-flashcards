{
  "private": true,
  "scripts": {
    "build": "markdown-flashcards build",
    "dev": "nodemon -w '../../packages/cli/dist/*.js' --exec \"markdown-flashcards ./slides.md --open=false --log=info\"",
    "export": "markdown-flashcards export",
    "export-notes": "markdown-flashcards export-notes"
  },
  "devDependencies": {
    "@markdown-flashcards/cli": "workspace:*",
    "@slidev/theme-default": "^0.21.2",
    "@slidev/theme-seriph": "^0.21.3",
    "@markdown-flashcards/types": "workspace:*",
    "nodemon": "^2.0.20"
  }
}
