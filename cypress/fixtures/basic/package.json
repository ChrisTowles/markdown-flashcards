{
  "private": true,
  "scripts": {
    "dev": "nodemon -w '../../../packages/cli/dist/*.js' --exec 'slidev --log=info'",
    "build": "slidev build",
    "export": "slidev export"
  },
  "devDependencies": {
    "@markdown-flashcards/cli": "workspace:*",
    "@markdown-flashcards/theme-default": "workspace:*",
    "@slidev/theme-seriph": "^0.21.3",
    "@markdown-flashcards/types": "workspace:*",
    "nodemon": "^2.0.20"
  }
}
